{
  "name": "whale-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Whale wallet tracking x402 API - track significant wallet movements on-chain",
  "workspaces": [
    "shared",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run build:packages && bun run build:api",
    "build:packages": "bun run --cwd shared build && bun run --cwd packages/wallets build && bun run --cwd packages/events build && bun run --cwd packages/indexer build",
    "build:api": "bun run --cwd packages/api build",
    "dev": "bun run --cwd packages/api dev",
    "start": "bun run --cwd packages/api start"
  },
  "keywords": [
    "whale",
    "crypto",
    "x402",
    "ethereum",
    "tracking"
  ],
  "license": "MIT",
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/better-sqlite3": "^7.6.13",
    "typescript": "^5.9.3"
  }
}
